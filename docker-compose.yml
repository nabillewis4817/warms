services:
  db:
    image: postgres:16
    container_name: warms-postgres
    environment:
      POSTGRES_DB: warms
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: MacKenzie
    ports:
      - "5432:5432"
    volumes:
      - warms_pgdata:/var/lib/postgresql/data

volumes:
  warms_pgdata:
